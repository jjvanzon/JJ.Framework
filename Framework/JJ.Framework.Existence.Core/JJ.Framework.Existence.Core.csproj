<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <Version>3.3.$(BuildNum)</Version>
    <!-- .NET targeting is good for single-exe deployment verification, but omits hitting .NET Standard 2.1 code with .NET 5 tests. -->
    <!--<TargetFrameworks>net10.0;net9.0;net8.0;net7.0;net6.0;net5.0;netstandard2.1;netstandard2.0</TargetFrameworks>-->
    <TargetFrameworks>net10.0;net9.0;net8.0;net7.0;net6.0;netstandard2.1;netstandard2.0</TargetFrameworks>
    <Description>Has(text), Coalesce(a,b,c), list.Where(FilledIn) to replace garbage like !Equals(x, default(T?)) &amp;&amp; !Equals(x, default(T)) and string.IsNullOrWhiteSpace(text). Simple fallbacks and emptiness checks, so you can code it like you mean it.</Description>
    <PackageTags>null; empty; whitespace; guards; coalesce; checks; is; in; filled; nully; utility; has; filledin; isnully; contains</PackageTags>
    <RepositoryUrl>https://github.com/jjvanzon/JJ.Framework/tree/main/Framework/JJ.Framework.Existence.Core</RepositoryUrl>
    <PackageProjectUrl>https://github.com/jjvanzon/JJ.Framework/tree/main/Framework/JJ.Framework.Existence.Core</PackageProjectUrl>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="JetBrains.Annotations" Version="2025.2.4" />
    <PackageReference Include="System.Collections.Immutable" Version="10.0.2" />
    <PackageReference Include="System.Memory" Version="4.6.3" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\JJ.Framework.Common.Core\JJ.Framework.Common.Core.csproj" />
    <ProjectReference Include="..\JJ.Framework.Common\JJ.Framework.Common.Legacy.csproj" />
  </ItemGroup>

  <Import Project="..\JJ.Framework.PlatformCompatibility.Core\JJ.Framework.PlatformCompatibility.Core.projitems" Label="Shared" />
  <Import Project="..\JJ.Framework.SharedProject.Core\JJ.Framework.SharedProject.Core.projitems" Label="Shared" />

  <PropertyGroup>
    <NoWarn>$(NoWarn);IDE0130</NoWarn> <!-- Namespace does not match folder structure-->
  </PropertyGroup>
</Project>
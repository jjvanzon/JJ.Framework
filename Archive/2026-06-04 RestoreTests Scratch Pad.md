RestoreTests Scratch Pad
========================

```cs        
        string exceptionMessage = "";
        try
        {
            var build = Build(opt);
        }
        catch (Exception ex)
        {
            exceptionMessage = ex.Message;
        }
```
Testing.Core.Tests Scratch Pad
==============================

```cs
    [TestMethod]
    public void AssertCore_ThrowsException_Methods()
    {
        ThrowsException(ThrowingAction);
        ThrowsException(ThrowingAction, "boom");
        ThrowsException(ThrowingAction, typeof(InvalidOperationException));
        ThrowsException(ThrowingAction, typeof(InvalidOperationException), "boom");
        ThrowsException<InvalidOperationException>(ThrowingAction);
        ThrowsException<InvalidOperationException>(ThrowingAction, "boom");
    }

        ThrowsExceptionOnOtherThread(ThrowingAction);


    [TestMethod]
    public void AssertCore_AreSame_ValueType()
    {
        const int same = 1;

        Throws(() => AreSame(1, same),         "AreSame failed", "same");
        Throws(() => AreSame(1, same, "oops"), "AreSame failed", "same", "oops");
    }
   
    // TODO: Fails, but is a weird case: reference equals for value types.
    /*
    [TestMethod]
    public void AssertCore_NotSame_ValueType()
    {
        const int same = 1;

        NotSame   (1, same);
        AreNotSame(1, same);
        NotSame   (1, same, "oops");
        AreNotSame(1, same, "oops");

        Throws(() => NotSame   (1, 2),         "NotSame failed", "2");
        Throws(() => AreNotSame(1, 2),         "NotSame failed", "2");
        Throws(() => NotSame   (1, 2, "oops"), "NotSame failed", "2", "oops");
        Throws(() => AreNotSame(1, 2, "oops"), "NotSame failed", "2", "oops");
    }
    */
```
